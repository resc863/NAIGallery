Folder: Converters

Purpose
- Small XAML converters used by bindings.

Files
- `NullToVisibilityConverter`: shows a placeholder when a bound value is null.

Notes
- Keep converters stateless and fast; they run on the UI thread.