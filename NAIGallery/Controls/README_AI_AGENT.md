Folder: Controls

Purpose
- Reusable light-weight XAML controls.

Files
- `AspectPresenter`: maintains a fixed aspect ratio for its content to reduce layout shifts while thumbnails progressively upgrade.

Usage
- Bind `AspectRatio` to `ImageMetadata.AspectRatio` and constrain either width or height.